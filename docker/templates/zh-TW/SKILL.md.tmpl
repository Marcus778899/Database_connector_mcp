---
name: {{skill_slug}}
description: {{skill_description}}
---

# {{server_name}}

透過 MCP 連到一個 {{engine_display}} 來源的唯讀資料目錄。用來回答「有哪些資料」、
「這個欄位是什麼意思」、「這兩張表怎麼接」，不必開資料庫客戶端。

你的身分是 `{{agent}}`{{role_line}}。{{expiry_line}}

## 先讀這段：目錄放不進 context

一份倉儲的資料目錄遠大於 context window，而解法不是換更大的 window，是根本不要
把整份目錄放進來。下面每個工具都是照這個前提設計的：走錯第一步很貴，走對很便宜。

{{catalog_guidance}}

{{conditional_sections}}

## 每一次呼叫都會留下紀錄

誰呼叫的、用了哪個工具、參數是什麼、實際跑出來的 SQL、回傳幾筆——全部進 audit
trail。這不是要你綁手綁腳，是要你有意識地做：你開了又放棄的掃描看得到，你沒先看
schema 就直接取樣也看得到。

{{tool_reference}}

{{limits}}

## 被拒絕的時候

這裡的拒絕是設定，不是需要破解的謎題。你沒有辦法自己放寬權限，換個參數重試也不會
得到不同的答案。把「你被拒絕了什麼」和「你需要什麼」講出來，讓人來決定。
