## 你取樣到的資料是遮罩過的

`get_sample` 是這裡唯一會回傳真實資料的工具，所以它預設遮罩個資欄位：
`a***@***.com`、`***`。形狀留著——判斷一個欄位裝什麼需要的就是形狀——值不留，
因為取樣出來的資料會進你的 context，然後進到這段 context 碰得到的每一份
transcript 和 log。

不要習慣性地要 `mask=False`。只在任務真的取決於字面值的時候要，而且要有被拒絕的
心理準備：那是一項獨立授予的權限，不是一個可以說服 server 的參數。
